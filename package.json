{
  "name": "examcam",
  "version": "1.0.0",
  "description": "Exam camera records from webcam and outputs 1fps mp4",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "MobiusInstitute",
  "icon": "assets/icon.ico",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.1.0",
    "electron-builder": "^23.6.0"
  },
  "dependencies": {
    "electron-store": "^8.1.0",
    "electron-updater": "^5.3.0",
    "ffmpeg": "^0.0.4",
    "ffmpeg-static": "^5.1.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.2",
    "fs-extra": "^10.1.0"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/EdelweissPirate/examcam.git"
  }
}
